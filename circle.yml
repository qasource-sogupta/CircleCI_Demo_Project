## Set the artifacts
general:
  artifacts:
    - "./tests/screenshots" #Save the screenshots for the tests
 
## Customize dependencies
dependencies:
  pre:
    - sudo pip install selenium	
 
## Running Tests
test:
   override:
    - python tests/dummy_Script.py
